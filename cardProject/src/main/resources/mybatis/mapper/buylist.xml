<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.definejae234.cardproject.buylist.dao.BuyListDao">
    <select id="findBuylistDataByMemberId" resultType="BuyListDto"
    parameterType="long">
        SELECT * FROM member_card_buylist_jpa WHERE mem_id = #{mem_id}
    </select>

    <select id="topFiveList" resultType="BuyListDto" parameterType="map">
        SELECT *
        FROM (
        SELECT
        card_name,
        card_corp,
        card_image,
        COUNT(*) AS purchase_count,
        RANK() OVER (ORDER BY COUNT(*) DESC) AS rank
        FROM
        MEMBER_CARD_BUYLIST_JPA
        WHERE
        card_cate = #{cardcate}
        GROUP BY
        card_name, card_corp, card_image
        ) ranked_cards
        WHERE ranked_cards.rank &lt;= #{limit}
    </select>
</mapper>